name: CI and Firebase Preview

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ci-preview-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  frontend:
    name: Frontend CI
    runs-on: ubuntu-latest
    env:
      DEPLOY_TARGET: ci
      VITE_FIREBASE_API_KEY: ci-placeholder
      VITE_FIREBASE_AUTH_DOMAIN: ci.invalid
      VITE_FIREBASE_PROJECT_ID: ci-placeholder
      VITE_FIREBASE_STORAGE_BUCKET: ci.invalid
      VITE_FIREBASE_MESSAGING_SENDER_ID: '000000000000'
      VITE_FIREBASE_APP_ID: '1:000000000000:web:ci'
      VITE_FIREBASE_MEASUREMENT_ID: G-CI00000000
      VITE_FINNHUB_API_KEY: ci-placeholder
      VITE_STRIPE_PUBLISHABLE_KEY: pk_test_ci_placeholder
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - uses: actions/setup-node@v5
        with:
          node-version: '20'
          cache: npm
      - name: Install frontend dependencies
        run: npm ci
      - name: Validate explicit Vite configuration
        run: npm run validate:env
      - name: Lint frontend
        run: npm run lint
      - name: Build frontend
        run: npm run build

  functions:
    name: Functions CI
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: functions
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - uses: actions/setup-node@v5
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: functions/package-lock.json
      - name: Install Functions dependencies
        run: npm ci
      - name: Build Functions
        run: npm run build
      - name: Run Functions unit tests
        run: npm test -- --runInBand

  preview:
    name: Firebase TEST preview and backend report
    if: >-
      github.event_name == 'pull_request' &&
      github.event.pull_request.head.repo.full_name == github.repository
    needs:
      - frontend
      - functions
    runs-on: ubuntu-latest
    environment: staging
    permissions:
      contents: read
      checks: write
      pull-requests: write
    env:
      DEPLOY_TARGET: staging
      FIREBASE_PROJECT_ID: ${{ vars.FIREBASE_PROJECT_ID }}
      VITE_FIREBASE_API_KEY: ${{ vars.VITE_FIREBASE_API_KEY }}
      VITE_FIREBASE_AUTH_DOMAIN: ${{ vars.VITE_FIREBASE_AUTH_DOMAIN }}
      VITE_FIREBASE_PROJECT_ID: ${{ vars.FIREBASE_PROJECT_ID }}
      VITE_FIREBASE_STORAGE_BUCKET: ${{ vars.VITE_FIREBASE_STORAGE_BUCKET }}
      VITE_FIREBASE_MESSAGING_SENDER_ID: ${{ vars.VITE_FIREBASE_MESSAGING_SENDER_ID }}
      VITE_FIREBASE_APP_ID: ${{ vars.VITE_FIREBASE_APP_ID }}
      VITE_FIREBASE_MEASUREMENT_ID: ${{ vars.VITE_FIREBASE_MEASUREMENT_ID }}
      VITE_FINNHUB_API_KEY: ${{ vars.VITE_FINNHUB_API_KEY }}
      VITE_STRIPE_PUBLISHABLE_KEY: ${{ vars.VITE_STRIPE_PUBLISHABLE_KEY }}
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - uses: actions/setup-node@v5
        with:
          node-version: '20'
          cache: npm
      - name: Require an isolated Firebase project before deploying a preview
        id: test-target
        run: |
          if [ -z "$FIREBASE_PROJECT_ID" ] || [ "$FIREBASE_PROJECT_ID" = 'accelrestaurant-d2c1f' ]; then
            echo 'deploy=false' >> "$GITHUB_OUTPUT"
            echo '::warning::TEST preview not deployed: staging must use a separate non-production Firebase project.'
            echo 'TEST preview not deployed. No isolated Firebase project is configured; real Stripe and Storage release checks remain blocked.' >> "$GITHUB_STEP_SUMMARY"
          else
            echo 'deploy=true' >> "$GITHUB_OUTPUT"
          fi
      - name: Validate preview Firebase target and service account
        if: steps.test-target.outputs.deploy == 'true'
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: |
          npm run validate:env
          if [ -z "$FIREBASE_PROJECT_ID" ]; then
            echo '::error::The staging GitHub environment is missing FIREBASE_PROJECT_ID.'
            exit 1
          fi
          if [ -z "$FIREBASE_SERVICE_ACCOUNT" ]; then
            echo '::error::The staging GitHub environment is missing the FIREBASE_SERVICE_ACCOUNT secret.'
            exit 1
          fi
      - name: Install frontend dependencies
        if: steps.test-target.outputs.deploy == 'true'
        run: npm ci
      - name: Build Firebase Hosting preview
        if: steps.test-target.outputs.deploy == 'true'
        run: npm run build
      - name: Deploy Firebase Hosting preview channel
        if: steps.test-target.outputs.deploy == 'true'
        id: hosting-preview
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          projectId: ${{ vars.FIREBASE_PROJECT_ID }}
          expires: 14d

      - name: Report actual backend compatibility (Hosting preview is not a backend release)
        if: env.FIREBASE_PROJECT_ID != ''
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: npm ci --prefix functions && node scripts/journey-release.mjs --preview
      - name: Install public preview browser checks
        if: steps.test-target.outputs.deploy == 'true'
        run: npm ci --prefix tests/hig && cd tests/hig && npx playwright install --with-deps chromium
      - name: Verify plan comparison on the deployed preview without service mocks
        if: steps.test-target.outputs.deploy == 'true'
        env:
          PREVIEW_URL: ${{ fromJSON(steps.hosting-preview.outputs.urls)[0] }}
        run: node tests/journey/preview.mjs
      - name: Retain actual preview and dependency evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: journey-live-preview-evidence
          path: test-results
          retention-days: 7
